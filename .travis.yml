language: php

php:
- 7.0
- hhvm

before_script:
  - apt-get install -y libssl-dev libcurl4-openssl-dev pkg-config libssl-dev libsslcommon2-dev                                           
  - apt-get install -y mongodb git                                                                                                       
  - apt-get install php-mongodb                                                                                                          
  - pecl install mongodb
  - composer install

script:
  - mkdir -p build/logs
  - php bin/codecept.phar run unit

